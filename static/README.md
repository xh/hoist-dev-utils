# Build-related files

* `index.html` - rendered by Rsbuild's `html-rspack-plugin` as the template index.html for each client app. Note that this
  had been sourced from hoist-react, but as of hoist-dev-utils >= v8.1 is included with this project for simplicity.